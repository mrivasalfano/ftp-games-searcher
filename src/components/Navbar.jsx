import React, { Component } from 'react';

class Navbar extends Component {
    render() {
        return (
            <header className="fixed top-0 w-screen">
                <nav className="w-full h-14 bg-red-500">Navbar</nav>
            </header>
        );
    }
}

export default Navbar;